# Mobile apps 9.9.0 — APK/HAR/PCAP analysis

Status: Completed (runtime UI + API parity backlog documented)

Date: 2026-07-15
Scope: «Мой Дом» (`ru.inetra.intercom`) and «Умный Дом.ру»
(`com.ertelecom.smarthome`)

## Evidence and confidence

| Tier | Evidence | What it proves |
|---|---|---|
| Runtime | Decrypted HAR: password auth, SMS auth, bootstrap, main tabs, camera view | exact methods, paths, query/body shapes and status codes for «Мой Дом» 9.9.0 |
| Network | PCAPdroid full-flow PCAP | DNS/TLS hosts, SIP sequence and encrypted video transport; HTTP paths remain encrypted |
| Static | Original signed APKs + JADX/apktool diff | manifest, UI/features, DTOs and candidate Retrofit endpoints for both flavours |
| Store | RuStore metadata | release version/date, versionCode and SDK metadata |

Static-only endpoints below are not promoted to the canonical API reference
unless the same call was observed in HAR (ADR-0006).

## Artifact verification

| App | Version | SDK (min/target) | Original signer SHA-256 | Artifact SHA-256 |
|---|---:|---:|---|---|
| Мой Дом | `9.9.0` (`90900020`) | 32 / 36 | `42b32c89…de74a54` | base APK `41871d6b…6a8589` |
| Умный Дом.ру | `9.9.0` (`90900020`) | 23 / 36 | `3cadb606…430980d` | APK `a1d03b80…c0a9559` |

The 9.9.0 signers match the repository's original baselines (MyHome 9.7.0
and Smart Home 9.3.0). The MyHome base pulled read-only from the updated
device is byte-identical to the downloaded base APK.

Store metadata:

- [Мой Дом in RuStore](https://www.rustore.ru/catalog/app/ru.inetra.intercom)
  — published 2026-07-04.
- [Умный Дом.ру in RuStore](https://www.rustore.ru/catalog/app/com.ertelecom.smarthome)
  — published 2026-06-23.

## What changed since the baselines

### Common changes in both flavours

- New Stories/banner communication layer, including advertising labels,
  advertiser metadata and metrics.
- New VPN/service-pipe warning screen. The client recognizes an HTML block
  page by `REQUEST-ID`, IP and datetime fields and shows a dedicated warning
  that the app is unavailable outside Russia or while VPN is enabled.
- Access-key notification status DTO and settings strings.
- No permission changes. Target SDK stays 36.
- New activities: `VpnWarningActivity` and `StoriesActivity`.

The 9.9.0 first-party class inventories converge: 952 shared paths; the only
flavour-only path is Smart Home's generated `R.java`. The relevant static
endpoint-string sets are identical (449 each), so both apps now use one very
close shared codebase with branding/auth differences.

### Мой Дом 9.7.0 → 9.9.0

- `versionCode`: `90700000` → `90900020`.
- `minSdk`: 29 → 32 (Android 12L/API 32).
- 31 first-party class paths added, none removed.
- New access-key notification preferences and Stories/VPN UI.

### Умный Дом.ру 9.3.0 → 9.9.0

- `versionCode`: `90300010` → `90900020`.
- `minSdk` remains 23.
- New activities for key notification, key activation and entrance settings.
- Expanded paid «Уведомления по ключу» subscription/FAQ/cancel UI.
- Access-key API strings moved from legacy `/rest/v1/accesskeys` to the
  `mh-access-key` mobile service (static evidence only).

## Runtime API findings — Мой Дом 9.9.0

### Auth flows

Password flow:

1. `GET /auth/v2/login/{login}` → 200.
2. `POST /auth/v2/auth/{login}/password` with
   `{login,timestamp,hash1,hash2}` → token response.

SMS flow:

1. `GET /auth/v2/login/{phone}` → 300 and contract list.
2. `POST /auth/v2/confirmation/{phone}` with contract/place fields → 200.
3. `POST /auth/v3/auth/{phone}/confirmation` with
   `{accountId,operatorId,subscriberId,login,confirm1,confirm2}` → token response.

These contracts match `api.py`; no auth migration is required.

### Bootstrap and main screens

Observed after auth:

- `GET /rest/v3/subscriber-places`.
- `GET /rest/v1/places/{placeId}/cameras`.
- `GET /rest/v1/places/{placeId}/accesscontrols`.
- `GET /rest/v2/places/{placeId}/public/cameras`.
- `GET /rest/v1/places/{placeId}/screen-sections`.
- `GET /api/mh-customer/mobile/v1/customers/places/{placeId}/settings/screens`.
- `GET /rest/v1/stomp/available-features` and WebSocket `/events` (101).
- one `POST .../sipdevices` per access control.
- Events tab: `POST /rest/v1/events/search?page&sort` with `{placeIds}`.
- Cabinet/balance: `GET /api/mh-payment/mobile/v1/finance?placeId`.

The integration already uses every endpoint needed for its current entity
model. Events history remains a documented optional feature gap.

### New/changed exact contracts

- New communications bootstrap:
  `POST /api/mh-customer-communication/mobile/v1/communications` with
  `{appId,appVersion,appVersionCode,platform}` → `{banners,stories}`.
  It is app UI content and has no current HA entity use-case.
- Live video now explicitly sends
  `GET /rest/v1/forpost/cameras/{id}/video?LightStream=0&Format=H264`.
- `device-installations` is pre-auth, carries no Bearer and omits
  `deviceType`; `subscriberNotifications` is post-auth and includes
  `deviceType: MOBILE_APPLICATION`.

### PCAP-only transport findings

The provided 147.6-second PCAP is encrypted for HTTP but confirms:

- `myhome.proptech.ru` over TLS/443;
- three TLS video connections to `forpost-15.novotelecom.ru:18081`
  (about 7.5 MB inbound total);
- three parallel SIP registrations over UDP/5060, followed by Digest 401,
  successful 200 registration, NOTIFY traffic and renewals about every 27 s;
- server OPTIONS receive 400 responses from the stock client in this capture.

This persistent multi-registration differs intentionally from the
integration's register-on-ring strategy (ADR-0012); the current HA design
avoids permanent SIP bindings and relies on FCM for the ring trigger.

## AVD screen inventory and integration parity

The stock applications were exercised on the research AVD after authentication.
No account identifiers, addresses, names, phone numbers, QR contents or invite
tokens are retained in this report.

### Мой Дом 9.9.0

| Screen / action | Runtime result | Integration status |
|---|---|---|
| Home access controls | Open controls and common cameras | implemented |
| Events | Date, device and people filters; accepted/missed intercom calls | durable history not implemented |
| Event → archive | Opens camera archive at the event timestamp | archive playback not implemented |
| Archive timeline | play/pause/seek, event clips with duration, per-clip download | media-source/download not implemented |
| People | owner and guest list | underlying place-scoped people query exists; no PII entity by design |
| Add guest | live QR + share link | invitation action not implemented |
| Cabinet | service state, balance, next payment/date and payment link | balance/blocking entities and payment attributes implemented |
| Notifications | external CAPTCHA page | deliberately outside integration API scope |

The guest QR/link was a live access credential. It was neither decoded nor
copied into repository artifacts.

### Умный Дом.ру 9.9.0

Password authentication succeeded on the AVD. The test account exposed the same
four-tab core surface, but did not have an enabled access-key tariff or private
camera, so those screens could not provide runtime network fixtures. Their
feature code and Retrofit/DTO contracts are present in both signed 9.9.0 APKs.

### Candidate HA features

The implementation-ready research package is
[`docs/features/mobile-app-parity/`](../../docs/features/mobile-app-parity/README.md).
Exact contracts and their evidence tier live in the canonical
[`API reference`](../../docs/architecture/api-reference.md):

- event history, camera events, archive playback and clip download;
- owner-side guest invitation;
- access-key inventory and notification settings;
- private-camera motion sensitivity, recording, audio volume, mirror and PTZ.

Guest links, access-key codes and signed archive/download URLs are credentials;
none may be stored in entity state/attributes, recorder, diagnostics or logs.

## Static-only candidate endpoints

Observed in both 9.9.0 APKs but not all exercised in HAR:

- `POST /api/mh-customer-communication/mobile/v1/communications/metrics`.
- guest invite generation under `/api/mh-auth/mobile/v1/guests/link`;
- access-key list/add/delete/check/rename/reactivate/notification endpoints
  under `/api/mh-access-key/mobile/v1/rest/v1/access-keys`;
- personal-camera motion sensitivity, record mode, microphone/speaker volume,
  mirror and PTZ settings.

Status: candidate only. Exact static DTOs are documented in explicitly marked
sections of the API reference so an implementer does not need to repeat APK
reverse engineering, but every write path still requires a corresponding
screen HAR before support is merged (ADR-0006).

## Integration impact

Applied:

- runtime app identity → `9.9.0` / `90900020`;
- live video query mirrors explicit `Format=H264`;
- public device installation omits Bearer and `deviceType`;
- research probe and version-check tooling synchronized.

No change required:

- password/SMS auth hashing and bodies;
- places/access-controls/camera/public-camera/settings/finance paths;
- snapshot and SIP-credential response handling.

Follow-up diagnostic gap:

- the stock app distinguishes service-pipe/VPN/geoblock HTML from ordinary
  network failures; `http.py` currently reduces it to generic `ClientError`,
  and several API wrappers then return empty collections. See audit A-92.
